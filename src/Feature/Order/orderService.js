const coinType = (type) => {
    return type;
  };

const orderType = (type) => {
  return type;
};

const memoType = (type) => {
  return type;
};
  
  const orderService = {
    coinType,
    orderType,
    memoType
  };
  
  export default orderService;
  