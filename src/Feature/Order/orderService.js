const coinType = (type) => {
    return type;
  };

const orderType = (type) => {
  return type;
};

const memoType = (type) => {
  return type;
};

const avtarType = (type) => {
  return type;
};
  
  const orderService = {
    coinType,
    orderType,
    memoType,
    avtarType
  };
  
  export default orderService;
  